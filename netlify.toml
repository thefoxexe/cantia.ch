[build]
  command = "npx expo export -p web -c && node scripts/inject-seo-meta.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
